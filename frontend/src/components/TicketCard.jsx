import {Link} from 'react-router-dom';
export default function TicketCard({ticket}){return <Link className="ticket-card" to={`/tickets/${ticket.id}`}><div className="ticket-top"><strong>#{ticket.id} {ticket.subject}</strong><span className={`badge ${ticket.status.replaceAll(' ','-').toLowerCase()}`}>{ticket.status}</span></div><p>{ticket.description.slice(0,120)}{ticket.description.length>120?'...':''}</p><div className="meta"><span>Priority: {ticket.priority}</span><span>{ticket.customer_name||'You'}</span><span>{new Date(ticket.updated_at||ticket.created_at).toLocaleDateString()}</span></div></Link>}
